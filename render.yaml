services:
  - type: web
    name: study-agent-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash -c "uvicorn backend.app:app --host 0.0.0.0 --port ${PORT}"
    envVars:
      - key: GROQ_API_KEY
        sync: false
